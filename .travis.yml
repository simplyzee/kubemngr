language: go
go: '1.13.x'
env:
  - GO111MODULE=on
install:
  - go mod download
  - make build
deploy:
  - provider: script
    skip_cleanup: true
    script: curl -sL https://git.io/goreleaser | bash -s -- --rm-dist
    on:
      tags: true
